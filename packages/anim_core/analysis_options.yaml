include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
  errors:
    # The package boundary is load-bearing (docs/v3/04 §1): anim_core must stay
    # pure Dart so it can be published as the runtime and tested without Flutter.
    implementation_imports: error
